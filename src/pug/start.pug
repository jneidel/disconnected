extends layout

block styles
  link( rel="stylesheet" type="text/css" href="css/start.css" )

block content
  label( for="name" ) Name 
    span.fs-small(Zur eigenen Identifikation)
  input#name( type="text" )
  label( for="age" ) Alter 
    span.fs-small (Jetztiges Alter)
  input#age( type="number" )
  label(for="startingAge") Start Alter 
    span.fs-small (Das Alter mit welchem du mit Sozialen Medien, Online Spiele, Youtube/Netflix, etc. begonnen hast)
  input#startingAge( type="number")
  label(for="time") Zeit 
    span.fs-small (Täglicher Medienkonsum in Minuten)
  input#time( type="number")
  div
    label( for="male" ) Man
    input#male( type="radio" name="gender" style="margin-right: 10px;" )
    label( for="female" ) Frau
    input#female( type="radio" name="gender" checked )
  button#next Weiter

block scripts
  script( src="js/start.js" )
